# 多语言（i18n）贡献指南

本项目 UI 支持 简体中文（zh_CN，默认/源语言）、繁體中文（zh_TW）、日本語（ja_JP）、한국어（ko_KR）、English（en_US）。
本文面向开发者与翻译贡献者，介绍文案的读写约定、工具链与新增语言的完整流程。

## 一、架构总览

| 组件 | 位置 | 说明 |
|---|---|---|
| 翻译目录 | `assets/locales/{lang}/LC_MESSAGES/march7th.po`（+ 编译产物 `.mo`） | 标准 gettext；**msgid 即中文原文** |
| 模板 | `assets/locales/march7th.pot` | 源码提取结果 |
| 运行时 | `module/localization/` | `tr()` / `tn()` / `trc()`，`languages.py` 为语言注册表 |
| 工具链 | `tools/i18n/` | `python -m tools.i18n {extract,check,compile,prune}` |
| 多语言文档 | `assets/docs/{Base}_{语言代码}.md` | Tutorial / Workflow / FAQ / Changelog / TasksTable |

- **回退链**：目标语言 → zh_TW 简转繁（OpenCC `s2twp`，台湾用语，与 `*_zh_TW.md` 文档口径一致）→ en_US → 中文原文；UI 永远不会出现「缺失」标记。
- **`.mo` 随 `.po` 一起提交**（打包时 `assets/` 整目录分发），改了 `.po` 必须重新 `compile`，否则 `check` 报错。
- 运行期代码**绝不写翻译目录**，登记/补齐一律走工具链。

## 二、在代码中加一条新文案

| 场景 | 写法 |
|---|---|
| 普通 UI 文案 | `tr("中文文案")` |
| 计数文案（一天/两天…） | `tn("共 {count} 个", n)` —— 自动按语言选择单复数并填充 `{count}` |
| 同词消歧（按钮的「运行」/状态的「运行」） | `trc("button", "运行")` / `trc("status", "运行")` |
| 带变量 | `tr("正在获取 {name} 第 {page} 页").format(name=x, page=y)` |
| 写日志（`log.*` / `self._log` / `appendLog`） | **直接写中文原文，不套 tr()** |

约定细则：

1. **占位符必须命名**：用 `{name}` / `{count}`，禁止 `{}` / `{0}`（`check` 会拦截；译文需要调整语序时命名占位符才能自由摆放）。
2. **译文必须原样保留占位符**（`check` 强制校验）。
3. **日志记 msgid，UI 显示 `tr(msgid)`**：同一句话要进日志又要显示时，共用同一个字符串常量——日志调用点不套 `tr()`，UI 调用点套 `tr()`。
   CLI 同理：`--list` 之类的命令行界面输出属 UI，随 `ui_language` 显示（`main.py` 启动时会 `load_language()`）；只有日志固定中文原文。
4. 程序逻辑**禁止依赖本地化文本**（不要 `startswith(tr(...))` 之类的文本匹配），需要分支判断请用自定义异常类型。
5. **模块级常量不要存译文**：`tr()` 只在调用时查表，而模块级常量在 import 期只求值一次——
   写 `AVAILABLE_TASKS = {"main": tr("完整运行")}` 会把译文**冻结在启动语言**，切换语言后不会更新，
   而且这类值一旦被写进 `config.yaml` 就变成用户数据，重启也不会恢复。

   ```python
   # 错：import 期求值，语言切换后不更新
   TASK_NAMES = {"main": tr("完整运行")}

   # 对：常量存中文原文（msgid），显示时再翻译
   TASK_NAMES = {"main": "完整运行"}
   ...
   label = tr(TASK_NAMES[task_id])
   ```
   需要持久化到配置时，存的必须是**语言无关的键**（msgid 或 task_id），而不是译文。
   参考实现见 `app/card/card_edit_dialog.py` 的 `display_label()` / `stored_label()`。
   旧版本遗留的译文型配置（如 `home_cards` 里的旧译文）由同文件的 `migrate_home_cards()`
   在加载时一次性还原为 msgid（歧义译文原样保留），无需手工处理。
   `pytest` 有守护用例（`tests/test_module/test_i18n_catalog.py::TestNoModuleLevelTranslation`）。
6. **`tr(x)` 的 x 不是字符串字面量时，必须登记它的来源**：静态提取只看得到 `tr("字面量")`，
   看不到 `tr(表[key])` / `tr(数据字段)` 的取值，工具会漏掉它们——既不进 `.pot`（译者看不到），
   又会被 `prune` 当成死键删掉（界面回退中文）。做法见第七节 `TABLE_SOURCES` / `DATA_SOURCES`。

   判别口诀：**写 `tr(x)` 时，如果 `x` 不是引号里的字面量，就问一句「x 的取值从哪来」，然后把它登记进 `tools/i18n`。**
7. **包装器不翻译，调用点负责 `tr()`**：像 `_show_msg(parent, title, content)`、
   `InfoBar.error(title=…, content=…)`、`TeachingTip.create(title=…)` 这类包装器/组件的入参，
   一律由调用点传入**已翻译**文本：

   ```python
   # 对：调用点翻译，包装器只负责显示
   _show_msg(self, tr("结果"), tr("还没有选择区域呢"))
   InfoBar.error(title=tr("输入错误"), content=tr("{type} 不在可选项中").format(type=t))

   # 错：包装器对入参再翻译一次 —— 调用点已译的文本二次查表必然落空，
   #     除了白做一次查找，还会在 DEBUG 日志里刷「缺失翻译」
   def _show_msg(parent, title, content):
       MessageBox(tr(title), tr(content), parent)
   ```

   例外是「入参本身就是 msgid」的解析函数，例如 `app/card/card_edit_dialog.py::display_label()`
   （常量/配置里存的是中文原文，由它按当前语言解析）与 `module/localization.trc()`（回退 `tr()`）。

   两条约定都有 pytest 守护：`tests/test_module/test_ui_i18n_conventions.py`
   （界面调用里不得出现未套 `tr()` 的中文字面量；包装器不得翻译自己的入参）。
   例外范围在该测试文件顶部说明：日志调用按第 3 条记中文原文，
   通知正文（`notify()` / `send_notification_with_screenshot()`）目前整体保持中文，尚未决策是否本地化。

提交前流程：

```powershell
uv run python -m tools.i18n extract   # 把新文案并入 .pot/.po（tn 自动建复数条目）
uv run python -m tools.i18n compile   # 重新编译 .mo（必须随 .po 提交）
uv run python -m tools.i18n check     # 自检（CI 与 pytest 跑同一套规则）
uv run pytest
```

## 三、复数与上下文的目录形态

- `tn("共 {count} 个", n)` 在目录中是一条**复数条目**：
  - `msgid` = 单数形文案，`msgid_plural` = `单数形文案|plural`（`|plural` 只是与历史 JSON 目录互转的元数据后缀，**不要手工改动**）；
  - en_US 有 `msgstr[0]`（单数）/`msgstr[1]`（复数）两个形式；zh_CN / zh_TW / ja_JP / ko_KR 只有一个形式（复数形非空优先）。
  - **en_US 这类多形式语言不允许留空任一形式**：`polib` 编译时会把「有形式为空」的条目**整条丢弃**，
    运行期连另一个形式都取不到，界面直接回退中文原文。`check` 对此报 error。
- `trc("语境", "文案")` 使用 gettext 的 `msgctxt` 机制：

  - **语境字符串本身不是文案，不要翻译**；文案仍然登记成一条普通条目，供 `tr()` 回退与其它调用点复用；
  - `extract` 会为每个 `(语境, 文案)` 额外生成一条 `msgctxt` 条目：

    ```po
    #: app/xxx.py:10
    msgid "运行"          # 无上下文条目：tr("运行") 或 trc 未命中语境时使用
    msgstr "Run"

    #: app/xxx.py:11
    msgctxt "button"      # 语境条目：trc("button", "运行")
    msgid "运行"
    msgstr "Run Button"
    ```

  - 运行期按 gettext 的 `语境\x04文案` 键查表，与 `module/localization.trc()` 一致；
  - `check` 会校验每条语境都已登记（缺条目报 error）。当前生产代码尚未使用 `trc`，需要同词消歧时再用。

## 四、翻译工作流

本地直接编辑对应语言的 `.po` 文件（推荐 Poedit / VS Code PO 扩展），保存后运行 `python -m tools.i18n compile` 并与 `.po` 一起提交。

两条必须遵守的填写约定：

1. **没有译文就留空 `msgstr`，不要预填英文占位。**
   运行期回退链（目标语言 → zh_TW 简转繁 → en_US → 中文原文）会自动兜底，留空不会让界面变空。
   反过来，把 en_US 的英文抄进 ja_JP 只会让条目「看起来已翻译」：既不进待翻译列表、Weblate 上也看不到，
   日语用户实际看到的还是英文，而且没人会再来补。历史上这样堆过 496 条，清理时才暴露出来。
   `check` 对「译文与 en_US 完全相同」会告警，但无法识别 `msgstr == msgid` 的同形词（日语汉字词），
   这类只能靠人工判断。
2. **zh_TW 的新条目一律留空 `msgstr`，不要写 `msgstr = msgid`。**
   运行期把「msgstr 非空」视为已翻译命中（`module/localization/_translated_or_none()`），
   因此 zh_TW 抄中文原文会**跳过 OpenCC 简转繁回退**，繁体用户看到简体字：

   | msgstr | 繁体用户看到 |
   |---|---|
   | `msgstr ""`（正确） | `祕技 1 次`（运行期简转繁） |
   | `msgstr "秘技 1 次"`（照抄原文） | `秘技 1 次`（简体泄漏） |

   只有人工润色过的繁体译文才写进 `msgstr`。ja_JP / ko_KR 没有转换层，不受此条约束。
3. 标点跟随各自语言习惯：zh_CN / zh_TW / ja_JP 用全角（`，` `。` `：` `！` `？` `（）`），
   en_US / ko_KR 用半角（`, . : ! ? ()`）——「韩文译文用半角」是正确写法，不要当成缺陷去「修正」。

### Weblate 接入（社区认领翻译）

在 [Weblate](https://weblate.org) 新建项目后添加组件：

| 配置项 | 值 |
|---|---|
| 源代码仓库 | 本仓库地址 |
| 文件掩码（File mask） | `assets/locales/*/LC_MESSAGES/march7th.po` |
| 模板（Template for new translations） | `assets/locales/march7th.pot` |
| 源语言（Source language） | 简体中文（zh_CN） |
| 文件格式 | gettext PO 文件 |

注意本项目 gettext 的非常规设定：**msgid 为中文原文**（源语言即 zh_CN），译者在 msgstr 中填写目标语言译文即可。

## 五、新增一种语言（示例：ru_RU）

1. **建目录骨架**：复制 `assets/locales/en_US/LC_MESSAGES/` 为 `assets/locales/ru_RU/LC_MESSAGES/`，清空各条目 `msgstr`（或从 `.pot` 复制），改 `Language` 与 `Plural-Forms` 头；翻译后 `python -m tools.i18n compile`。
2. **注册表加一行**：`module/localization/languages.py` 的 `LANGS` 中添加 `ru_RU` 条目（native 显示名、QLocale 枚举名、文档后缀 `ru_RU`、复数规则）。
   → 设置界面语言下拉框、Qt 组件翻译器、缺失回退链、系统语言探测**全部自动生效**。
3. **（可选）文档翻译**：界面会加载 5 类文档，缺哪份就会回退中文基准（`check` 会告警）：
   `Tutorial_ru_RU.md`、`Workflow_ru_RU.md`、`FAQ_ru_RU.md`、`TasksTable_ru_RU.md`、`Changelog_ru_RU.md`
   （命名 `{Base}_{语言代码}.md`，与 locales 语言代码一致；行结构见第六节）。
4. `README_RU.md` 等入口文档按需补充。

## 六、多语言文档约定

- 命名：`assets/docs/{Base}_{语言代码}.md`（如 `Tutorial_ja_JP.md`），**第 1 行为 `# 标题`**（界面加载时会剥掉首行），中文基准版不带后缀。
- **译文文档的结构固定为**：第 1 行标题、第 2 行空行、第 3 行声明（如 `> 本文件由…翻译/转换…`）、第 4 行空行，正文从第 5 行开始。
  界面是按行号剥离的（`app/help_interface.py`、`app/changelog_interface.py`），行数错位会把声明渲染进正文。
  `TasksTable` 是纯表格，各语言都不带声明。
- zh_CN / zh_TW / ja_JP / ko_KR / en_US 目前**都有**这 5 类文档；`zh_TW` 由简体基准经 OpenCC `s2twp` 生成，
  **改动简体基准后必须重新生成**：`python -m tools.i18n docs-tw`（`--check` 只检查是否最新，pytest 有守护用例）。
- 代码中统一通过 `module.localization.languages.localized_doc_path(base_name)` 解析当前语言版本（自动回退中文），不要手写语言映射。
- `TasksTable` 等多语言表格文档由 `check` 校验各语言**表格行数一致**；`check` 还会在「界面会加载的文档缺少某语言版本」时告警，防止改漏一份。
- 更新日志的 en/ja/ko 版本按仓库约定**不随每次发版同步**，各文件第 3 行的声明会写明覆盖到哪个版本并给出中文版链接。

## 七、目录工具速查

| 命令 | 作用 |
|---|---|
| `python -m tools.i18n extract` | 扫描源码（`tr`/`tn`/`trc` 字面量）、`TABLE_SOURCES` 常量表与 `DATA_SOURCES` 数据源，并入 `.pot`/`.po`（tn 自动建复数条目，trc 自动建 msgctxt 条目） |
| `python -m tools.i18n compile` | 编译 `.po` → `.mo` |
| `python -m tools.i18n check` | 校验 `.pot`/`.po`/`.mo` 一致性、占位符、复数形式数与空形式、trc 语境、位置占位符禁令、英文占位告警、多语言文档 |
| `python -m tools.i18n prune` | 删除「未登记进 `.pot` 且不在白名单」的死条目（历史遗留），`--dry-run` 只统计 |
| `python -m tools.i18n docs-tw` | 由简体基准经 OpenCC `s2twp` 重新生成 `assets/docs/*_zh_TW.md`，`--check` 只检查是否最新 |

### 声明「扫不到的文案来源」（`tools/i18n/__init__.py`）

静态扫描看不到动态传入的取值，必须显式声明；声明同时是 `check` 的校验依据（取值必须都在 `.pot` 里），
也是 `prune` 不误删它们的保证：

```python
TABLE_SOURCES = [                     # 模块级常量表：(源码路径, ((变量名, 取哪些键), ...))
    ("module/workflow/__init__.py", (("STEP_TYPE_LABELS", None), ("CONDITION_TYPE_LABELS", None))),
    ("utils/tasks.py", (("AVAILABLE_TASKS", None),)),
    ("app/card/card_edit_dialog.py", (
        ("HOME_EXTRA_TASKS", None),
        ("DEFAULT_CARDS", ("title", "label", "menu_items")),   # 只取展示字段，跳过 icon / task_id
    )),
]
DATA_SOURCES = [                      # 取值经 tr() 传入的数据文件
    "assets/config/character_names.json",
    "assets/config/instance_names.json",
    "assets/config/special_programs.jsonc",   # special_programs[].display_name
]
```

两处都用 AST / JSON 读取，**绝不 import 业务模块**——否则会把 PySide6、qfluentwidgets 拖进工具链。

`tools/i18n/legacy_keys.txt` 是死键白名单，仅供「确认要保留、但静态提取暂时覆盖不到」的条目过渡使用；
正确做法是补进上面的两张表，而不是加白名单。
